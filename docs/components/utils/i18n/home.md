# 国际化

## Hidden Title {.md-hidden}

<DocBasicUsage code='<el-config-provider :locale="locale"></el-config-provider>' />

### 基础用法

:::demo 基础写法：`<el-config-provider :locale="locale">...</el-config-provider>`。属性说明：`locale` 示例值：`locale`，类型：Element Plus 语言包对象，默认值由绑定变量初始值决定；`teleported` 示例值：`false`，类型：boolean，可选值：`true` / `false`，默认值：`true`。本示例展示中文、英文语言包切换。文档示例中将日期、时间选择器弹层挂载到当前示例容器内，避免路由切换时遗留 body 弹层节点。
utils/i18n/base
:::

### 说明

- 该页面用于展示项目内国际化相关的基础用法。
