# test测试组件. 仅用于本地测试, 不属于任何组件

:::demo 基础写法：`<div>...</div>`。属性说明：`v-model` 示例值：`cc`，类型由绑定值决定，默认值由绑定变量初始值决定；`v-copy` 示例值：`cc`，类型/可选值/默认值按内部组件或 Element Plus 对应属性。本示例展示base配置，可以直接复制基础写法后按业务替换数据。
test/base
:::

### 说明

- 该页面仅用于本地调试和临时验证，不作为正式组件文档。
