# 布局组件

## Hidden Title {.md-hidden}

### 基础用法

:::demo 基础写法：`<sBasicLayout title="租户容量配额" class="w-200" :square="true">...</sBasicLayout>`。属性说明：`title` 示例值：`租户容量配额`，类型：string，默认值：`""`；`square` 示例值：`true`，类型/可选值/默认值按内部组件或 Element Plus 对应属性。本示例展示基础渲染和最小配置，可以直接复制基础写法后按业务替换数据。
company/quotaPie/base
:::

### 说明

- 用于展示配额类占比信息的饼图或环图。
