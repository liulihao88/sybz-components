# 布局组件

## Hidden Title {.md-hidden}

### 基础用法

:::demo 基础写法：`<SOverviewItem :details="details">...</SOverviewItem>`。属性说明：`details` 示例值：`details`，类型/可选值/默认值按内部组件或 Element Plus 对应属性；`type` 示例值：`system`，类型：string，可选值：`primary` / `success` / `warning` / `danger` / `info` / `text`，默认值：`default` 或 `""`。本示例展示基础渲染和最小配置，可以直接复制基础写法后按业务替换数据。
company/overviewItem/base
:::

### 说明

- 用于概览页中的摘要指标展示，适合首页统计卡片。
