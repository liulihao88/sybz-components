---
name: sybz-components
description: >-
  初始化
---

# sybz-components

## 迁移规则

- 将所有组件前缀改为 `s`，例如 `o-button` 改为 `s-button`。
- 保留所有组件、函数、API 和功能行为，只修改命名和路径。
- 将相关路径、文档、包名、示例和引用统一改为 `sybz`。
- 目标仓库为 `https://github.com/liulihao88/sybz-components`。
