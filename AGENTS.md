# Codex 项目规则

## UI 规范

- 所有的主题示例, 都要放到基础用法示例后面
- 改过的代码, 不要自动build或docsbuild, 因为本来就可以直接看到效果
- 每个对应的示例, 要把示例所要展示的属性名称, 还有都有哪些值, 默认值都显示出来
- 每次组件新增的theme="chenghua", 要保证星号也跟着加入
- 示例如果有默认值, 把默认值加在示例顶部的title里, 示例的简单用法也写在title下面的文字介绍里, 最后再写介绍
- 每个组件顶部, 都有一个基础用法, 并且有可复制的小icon, 用公共方法写

## TS 规范

- 公共组件, 要支持鼠标移入组件, 先显示当前组件的属性提示, 然后再显示element-plus的属性提示
- 组件的属性如果变更, 对应的ts, md文档, 测试代码和测试用例都要跟这变
